import { createTheme } from "@mui/material";

const simpleLightTheme = createTheme({
  palette: {
    type: "light",
    primary: {
      main: "#3f7cb5",
    },
    secondary: {
      main: "#f50057",
    },
  },
});

export default simpleLightTheme;
