import { createTheme } from "@mui/system";

export const mySimpleTheme = createTheme({
  palette: {
    type: "dark",
    primary: {
      main: "#3f7cb5",
    },
    secondary: {
      main: "#f50057",
    },
  },
});
