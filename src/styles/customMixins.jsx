function customMixins({ palette, shadows, transactions }) {
  return {
    flexCenter: {
      display: "flex",
      alignContent: "center",
      alignItems: "center",
      flexWrap: "wrap",
    },
  };
}

export default customMixins;
